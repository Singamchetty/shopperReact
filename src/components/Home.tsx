import React, { memo, useState } from 'react';
import axios from 'axios';

const Home = memo(() => {
    

    return (
        <div>
          Home    
        </div>
    );
});

export default Home;